# ============================================================
# MedQLoRA — Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# ── Core MLX stack (Apple Silicon native) ───────────────────
mlx>=0.16.0
mlx-lm>=0.16.0           # MLX-LM: model loading, generation, LoRA training

# ── HuggingFace ecosystem ────────────────────────────────────
huggingface_hub>=0.23.0  # model download, push to Hub
datasets>=2.19.0          # load HF datasets
transformers>=4.41.0      # tokenizers, model configs
tokenizers>=0.19.0

# ── Evaluation metrics ───────────────────────────────────────
rouge-score>=0.1.2        # ROUGE-L for answer quality
numpy>=1.26.0
scikit-learn>=1.4.0       # F1, accuracy metrics

# ── Training utilities ───────────────────────────────────────
wandb>=0.17.0             # experiment tracking (optional, graceful fallback)
pyyaml>=6.0               # config.yaml parsing
tqdm>=4.66.0              # progress bars
rich>=13.7.0              # beautiful terminal output

# ── Inference & API ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── Demo App ─────────────────────────────────────────────────
gradio>=4.36.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0      # .env file loading
click>=8.1.0              # CLI interface for chat.py
httpx>=0.27.0             # async HTTP client
